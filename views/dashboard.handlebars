<section class="row">
  {{#each blogs as |blog|}}
  {{> user-blog-details}}
  {{/each}}

  <div class="col-12 col-md-8 border border-3 border-dark rounded m-4 text-center">
    <header class="bg-dark p-2">
    <a href="/dashboard/new" class="col-12 col-md-8 p-2">New Post</a>
    </header>
  </div>
</section>